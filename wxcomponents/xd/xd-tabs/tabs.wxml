<view class="wrapper wrapper-tabs class-tab">
<view class="tabs-header">
	<view wx:key="index" class="{{activeIndex == index ? 'lable active' : 'lable'}}" 
	wx:for="{{heads}}" data-index="{{index}}" 
	bindtap="changeActive">
	<view class="label-text class-tab">{{item}}<view class="border-active"></view></view>
	</view>
	
</view>
  <slot></slot>
</view>